@model IEnumerable<employeeManagement_EF.Models.EF.Deptinfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DLocation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DLocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
